/** Skrzynka e-mail w CRM — wyłączona w UI i na serwerze (brak harmonogramu IMAP). */
export const CRM_INBOX_ENABLED = false;

/** Sync IMAP → Supabase — nie uruchamiać (scripts/uninstall-crm-mail-sync.ps1 na serwerze). */
export const CRM_MAIL_SYNC_ENABLED = false;
